<UserControl x:Class="TimetableManager.WPF.StatisticsTimetableDataControls.StatisticUserControl.Tab_Stat_Student"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimetableManager.WPF.StatisticsTimetableDataControls.StatisticUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dataGridstd" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,225">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Year 1 Groups" Binding="{Binding Year1}" />
                <DataGridTextColumn Header="Year 2 Groups" Binding="{Binding Year2}" />
                <DataGridTextColumn Header="Year 3 Groups" Binding="{Binding Year3}" />
                <DataGridTextColumn Header="Year 4 Groups" Binding="{Binding Year4}" />
            </DataGrid.Columns>


        </DataGrid>

    </Grid>
</UserControl>
